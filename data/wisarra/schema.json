{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Wisarra agricultural market price CSV snapshot row",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "name",
    "location",
    "marketplace",
    "min_price",
    "max_price",
    "currency",
    "quantity",
    "unit",
    "scraped_at",
    "source_url"
  ],
  "properties": {
    "name": {"type": "string", "minLength": 1, "description": "Product name as published by Wisarra."},
    "location": {"type": "string", "minLength": 1, "description": "City or region as published by Wisarra."},
    "marketplace": {"type": "string", "minLength": 1, "description": "Specific market as published by Wisarra."},
    "min_price": {"type": "string", "pattern": "^(?:[0-9]+)?$", "description": "Non-negative integer encoded as CSV text, or empty when unavailable."},
    "max_price": {"type": "string", "pattern": "^(?:[0-9]+)?$", "description": "Non-negative integer encoded as CSV text, or empty when unavailable."},
    "currency": {"type": "string", "minLength": 1},
    "quantity": {"type": "string", "minLength": 1},
    "unit": {"type": "string", "minLength": 1},
    "scraped_at": {"type": "string", "format": "date-time"},
    "source_url": {"type": "string", "format": "uri"}
  }
}
